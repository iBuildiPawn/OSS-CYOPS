# ===========================================
# CYOPS PRODUCTION ENVIRONMENT
# ===========================================
# IMPORTANT: Copy this file to .env.production and update all values
# Never commit .env.production to version control

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cyops_prod
DB_USER=cyops_admin
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_123!@#
DB_SSL_MODE=require

# ===========================================
# REDIS CONFIGURATION
# ===========================================
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD_456!@#

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
PORT=8080
GO_ENV=production

# ===========================================
# SECURITY SECRETS (CRITICAL - MUST CHANGE)
# ===========================================
# Generate strong random values using:
# openssl rand -base64 32

# JWT Secret (minimum 32 characters)
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING_GENERATE_WITH_OPENSSL_RAND

# Session Secret (minimum 32 characters)
SESSION_SECRET=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING_GENERATE_WITH_OPENSSL_RAND

# Encryption Key (exactly 32 characters for AES-256)
ENCRYPTION_KEY=CHANGE_THIS_EXACTLY_32_CHARS!!

# ===========================================
# ADMIN USER CONFIGURATION
# ===========================================
# Initial admin user created on first deployment
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=CHANGE_THIS_ADMIN_PASSWORD_789!@#
ADMIN_NAME=System Administrator

# ===========================================
# EMAIL CONFIGURATION (SMTP)
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
FROM_EMAIL=noreply@example.com

# ===========================================
# CORS CONFIGURATION
# ===========================================
# CRITICAL: Comma-separated list of allowed origins (NO SPACES after commas)
# NEVER use wildcard (*) in production - always whitelist specific domains
# Include both HTTP and HTTPS only if you need to support both protocols
# 
# Production (HTTPS + Internal IP):
CORS_ORIGINS=https://cyops.example.com,https://www.cyops.example.com,http://192.168.20.21,https://192.168.20.21
#
# Local Development (uncomment for local testing):
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://192.168.20.21
#
# Note: Internal IP (192.168.20.21) supports both HTTP and HTTPS for flexibility

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
# CRITICAL: Leave empty or use base domain URL (without /api path)
# The application code already includes /api/v1 paths
# Empty value = relative URLs through NGINX (recommended for production)
NEXT_PUBLIC_API_URL=

# Alternative: Use full domain URL (no /api suffix)
# NEXT_PUBLIC_API_URL=https://cyops.example.com
# NEXT_PUBLIC_API_URL=https://192.168.20.21

# NextAuth Configuration
NEXTAUTH_URL=https://cyops.example.com
NEXTAUTH_SECRET=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING_GENERATE_WITH_OPENSSL_RAND

# For internal IP access:
# NEXTAUTH_URL=https://192.168.20.21

# ===========================================
# OPTIONAL: OAUTH PROVIDERS
# ===========================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ===========================================
# MONITORING & LOGGING (Optional)
# ===========================================
# SENTRY_DSN=
# LOG_LEVEL=info
# ENABLE_METRICS=true

# ===========================================
# BACKUP CONFIGURATION (Optional)
# ===========================================
# BACKUP_ENABLED=true
# BACKUP_SCHEDULE=0 2 * * *
# BACKUP_RETENTION_DAYS=30
# S3_BUCKET=cyops-backups
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
