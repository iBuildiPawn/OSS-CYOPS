# CYOPS Backend API Configuration
CYOPS_BACKEND_URL=http://localhost:8080/api/v1

# Authentication - Use API Key (recommended) or JWT Token (legacy)
# API Key (recommended for MCP clients - long-lived, no expiration)
CYOPS_API_KEY=cyops_mcp_your_api_key_here

# JWT Token (legacy - expires after 24 hours)
# CYOPS_API_TOKEN=your_jwt_token_here

# HTTP/SSE Transport Configuration (optional - for HTTP-based clients)
# Port for HTTP/SSE server (default: 3001)
MCP_PORT=3001

# CORS origins for HTTP/SSE server (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# How to get an API key (recommended):
# 1. Login to CYOPS frontend at http://localhost:3000
# 2. Navigate to Admin > MCP Server
# 3. Click "Create API Key"
# 4. Select scopes (vulnerabilities:read, vulnerabilities:stats, assets:read recommended)
# 5. Copy the generated API key (starts with cyops_mcp_)
# 6. Paste it here as CYOPS_API_KEY

# How to get a JWT token (legacy, not recommended):
# 1. Login to CYOPS frontend or use API
# 2. POST /api/v1/auth/login with credentials
# 3. Copy the JWT token from response
# 4. Paste it here as CYOPS_API_TOKEN (expires after 24 hours)

# Transport Types:
# - Stdio (default): Use "npm start" for Claude Desktop and similar clients
# - HTTP/SSE: Use "npm run start:http" for web-based clients and remote access
