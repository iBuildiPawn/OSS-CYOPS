# ===========================================
# CYOPS ENVIRONMENT CONFIGURATION
# ===========================================
# This file works for both development and production
# Copy to .env for development or .env.production for production

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cyops_dev
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL_MODE=disable

# ===========================================
# REDIS CONFIGURATION
# ===========================================
REDIS_PASSWORD=redis_password

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
PORT=8080
GO_ENV=development
NODE_ENV=development
BUILD_TARGET=development

# ===========================================
# SECURITY SECRETS
# ===========================================
# Generate strong random values for production using:
# openssl rand -base64 32

JWT_SECRET=your-jwt-secret-change-in-production
SESSION_SECRET=your-session-secret-change-in-production
ENCRYPTION_KEY=your-32-character-encryption-key

# ===========================================
# ADMIN USER CONFIGURATION
# ===========================================
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin123!@#
ADMIN_NAME=System Administrator

# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=noreply@yourapp.com

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Development: Allow localhost and internal IP
# NEVER use wildcard (*) - always specify allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://192.168.20.21,https://192.168.20.21

# Production: Use your actual domain (copy from .env.production.example)
# CORS_ORIGINS=https://cyops.example.com,https://www.cyops.example.com,http://192.168.20.21,https://192.168.20.21

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
# For production (NGINX reverse proxy): Leave empty or use current domain
# The frontend will use relative URLs which go through NGINX
NEXT_PUBLIC_API_URL=

# For local development (direct backend access): Use backend URL
# NEXT_PUBLIC_API_URL=http://localhost:8080

NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-change-in-production

# Production examples:
# NEXT_PUBLIC_API_URL= (empty - uses relative URLs through NGINX)
# NEXT_PUBLIC_API_URL=https://192.168.20.21
# NEXT_PUBLIC_API_URL=https://cyops.example.com
# NEXTAUTH_URL=https://192.168.20.21
# NEXTAUTH_URL=https://cyops.example.com

# ===========================================
# MCP SERVER CONFIGURATION (Optional)
# ===========================================
# Get API key from: POST /api/v1/api-keys or frontend API Keys page
CYOPS_API_KEY=

# ===========================================
# OPTIONAL: OAUTH PROVIDERS
# ===========================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=